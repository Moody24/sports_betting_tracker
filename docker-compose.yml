services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist the SQLite database outside the container
      - db_data:/app/instance
    restart: unless-stopped

volumes:
  db_data:
